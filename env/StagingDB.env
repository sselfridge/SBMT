ASPNETCORE_ENVIRONMENT=Staging
DB_SERVER=localhost
DB_PORT=5551
DB_LOCAL_PORT=5551
DB_USER=postgres
DB_PASS=mypassword
DB_NAME=postgres

STRAVA_SECRET=
JWT_KEY=

# this file is needed to load to run `dotnet ef` commands against the stg DB.
# The other stage is the env for the app to access the DB from within docker